[package]
edition      = "2024"
name         = "waveshare-esp32s3-amoled-2-16"
rust-version = "1.88"
version      = "0.1.0"

[[bin]]
name = "waveshare-esp32s3-amoled-2-16"
path = "./src/bin/main.rs"
test = false

[[test]]
harness = false
name    = "hello_test"

[lib]
test = false

[dependencies]
esp-hal = { version = "~1.1.0", features = [
  "defmt",
  "esp32s3",
  "unstable",
] }

esp-rtos = { version = "0.3.0", features = [
  "defmt",
  "embassy",
  "esp-alloc",
  "esp-radio",
  "esp32s3",
] }

defmt                  = "1.0.1"
esp-bootloader-esp-idf = { version = "0.5.0", features = ["defmt", "esp32s3"] }

bt-hci = "0.8.0"
axp2101-dd = { version = "0.3.0", features = ["defmt"] }
cst92xx = { version = "0.1.0", features = ["defmt"] }
pcf85063a = { version = "0.1.1", features = ["defmt"] }
embassy-embedded-hal = "0.6.0"
embassy-executor = { version = "0.10.0", features = ["defmt"] }
embassy-sync = "0.8.0"
embassy-time = { version = "0.5.0", features = ["defmt"] }
embedded-hal-async = "1.0"
embedded-io = { version = "0.7.1", features = ["defmt"] }
embedded-io-async = { version = "0.7.0", features = ["defmt"] }
esp-alloc = { version = "0.10.0", features = ["defmt"] }
esp-radio = { version = "0.18.0", features = [
  "ble",
  "defmt",
  "esp-alloc",
  "esp32s3",
  "unstable",
] }
panic-rtt-target = { version = "0.2.0", features = ["defmt"] }
rtt-target = { version = "0.6.2", features = ["defmt"] }
trouble-host = { version = "0.6.0", features = ["gatt"] }

critical-section = "1.2.0"
static_cell      = "2.1.1"
bytemuck         = "1.25.0"
embedded-hal     = "1.0"
time             = { version = "0.3.47", default-features = false }
# The CO5300 renderer uses the dirty-region alignment API currently available
# on Slint's upstream development branch. Cargo.lock pins the exact commit.
slint = { git = "https://github.com/slint-ui/slint.git", branch = "master", default-features = false, features = [
  "compat-1-2",
  "libm",
  "renderer-software",
  "unsafe-single-threaded",
] }
embassy-futures = "0.1.2"

[dev-dependencies]
embedded-test = { version = "0.7.1", features = [
  "defmt",
  "embassy-010",
  "external-executor",
  "xtensa-semihosting",
] }

# For fine tuning these settings, please refer to https://doc.rust-lang.org/cargo/reference/profiles.html
[profile.dev]
# The default debug profile is too slow and too big for resource-constrained devices.
# Always build with some optimizations enabled.
opt-level = "s"

[profile.release]
codegen-units = 1     # LLVM can perform better optimizations using a single thread
debug         = 2     # prefer slower builds but better debugging experience
lto           = 'fat'
opt-level     = 's'

[build-dependencies]
slint-build = { git = "https://github.com/slint-ui/slint.git", branch = "master" }
